import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    paddingHorizontal: 16,
    fontSize: 20,
    borderRadius: 4,
    borderWidth: 2,
    minWidth: 240,
    height: 56,
  }
});
